package heroes.battle;import heroes.castle.Castle;import heroes.creatures.Creature;import heroes.creatures.CreatureGenerator;import heroes.player.Player;import java.util.ArrayList;import java.util.List;public class BattleField {    private static BattleField battleField = null;    private List<Cell> cells = new ArrayList<>();    static List<Creature> creatures = new ArrayList<>();    private final List<Player> players = new ArrayList<>();    private BattleField() {        System.out.println("Here is the battlefield!");        // here to initiate cells with objects of Cell class:        for (int i = 0; i <= 25; i++) {            cells.add(new Cell(i));        }        players.add(new Player(1, new Castle("Dungeon")));        players.add(new Player(2, new Castle("Dungeon")));    }    public static BattleField getInstance() {        if (battleField == null) {            battleField = new BattleField();        }        return battleField;    }}