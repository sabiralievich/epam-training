package heroes.castle;import heroes.creatures.Creature;import heroes.creatures.CreatureGenerator;import java.util.ArrayList;import java.util.Arrays;import java.util.List;public class Castle {    String name;    String type;    private List<Creature> creatures = new ArrayList<>();    private List<NestHouse> nestHouses;    public Castle() {    }    public Castle(String name, String type) {        this.name = name;        this.type = type;    }    public Castle(String type) {        this.type = type;        for (int i = 0; i < 1; i++) {            creatures.add(CreatureGenerator.createCreature(type));        }        switch (type) {            case "Dungeon" -> nestHouses = Arrays.asList(new NestHouse("Cage"), new NestHouse("Grove"),                    new NestHouse("Attic"), new NestHouse("Eye Socket"), new NestHouse("Chapel"),                    new NestHouse("Labyrinth"), new NestHouse("Manticore Lair"),                    new NestHouse("Dragon Cave"));            case "Necropolis" -> nestHouses = Arrays.asList(); // implement when needed            default -> throw new IllegalStateException("Unexpected value: " + type);        }    }}